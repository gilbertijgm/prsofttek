_schema-version: "3.2"
ID: comsofttekacademiatkd
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comsofttekacademiatkd-destination-content
  type: com.sap.application.content
  requires:
  - name: comsofttekacademiatkd-destination-service
    parameters:
      content-target: true
  - name: comsofttekacademiatkd-repo-host
    parameters:
      service-key:
        name: comsofttekacademiatkd-repo-host-key
  - name: comsofttekacademiatkd-uaa
    parameters:
      service-key:
        name: comsofttekacademiatkd-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comsofttekacademiatkd_html_repo_host
          ServiceInstanceName: comsofttekacademiatkd-html5-srv
          ServiceKeyName: comsofttekacademiatkd-repo-host-key
          sap.cloud.service: comsofttekacademiatkd
        - Authentication: OAuth2UserTokenExchange
          Name: comsofttekacademiatkd_uaa
          ServiceInstanceName: comsofttekacademiatkd-xsuaa-srv
          ServiceKeyName: comsofttekacademiatkd-uaa-key
          sap.cloud.service: comsofttekacademiatkd
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: comsofttekacademiatkd-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comsofttekacademiatkd-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comsofttekacademiatkd.zip
      name: comsofttekacademiatkd
      target-path: resources/
- name: comsofttekacademiatkd
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comsofttekacademiatkd-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comsofttekacademiatkd-destination-service
    service-plan: lite
- name: comsofttekacademiatkd-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comsofttekacademiatkd-xsuaa-srv
    service-plan: application
- name: comsofttekacademiatkd-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comsofttekacademiatkd-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
